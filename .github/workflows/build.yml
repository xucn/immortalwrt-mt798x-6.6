name: Build OpenWRT

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install build dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential ccache libncurses-dev zlib1g-dev libssl-dev git wget flex bison

      - name: Update feeds
        run: |
          ./scripts/feeds update -a

      - name: Install feeds
        run: |
          ./scripts/feeds install -a
          ./scripts/feeds install -a -p passwall2

      - name: Configure
        run: |
          cp defconfig/mt7986-ax6000.config .config
          {
            echo "CONFIG_PACKAGE_luci-app-turboacc-mtk=y"
            echo "CONFIG_PACKAGE_turboacc-mtk=y"
            echo "CONFIG_PACKAGE_frpc=y"
            echo "CONFIG_PACKAGE_luci-app-frpc=y"
            echo "CONFIG_PACKAGE_cups=y"
            echo "CONFIG_PACKAGE_cups-filters=y"
            echo "CONFIG_PACKAGE_luci-app-cups=y"
            echo "CONFIG_PACKAGE_kmod-usb-printer=y"
            echo "CONFIG_PACKAGE_vlmcsd=y"
            echo "CONFIG_PACKAGE_luci-app-vlmcsd=y"
            echo "CONFIG_PACKAGE_luci-app-passwall2=y"
            echo "CONFIG_PACKAGE_passwall2=y"
            echo "CONFIG_PACKAGE_xray-core=y"
            echo "CONFIG_PACKAGE_sing-box=y"
            echo "CONFIG_PACKAGE_hysteria2=y"
            echo "CONFIG_PACKAGE_v2ray-core=y"
            echo "CONFIG_PACKAGE_v2ray-geoip=y"
            echo "CONFIG_PACKAGE_v2ray-geosite=y"
            echo "CONFIG_PACKAGE_geoview=y"
            echo "CONFIG_PACKAGE_shadowsocks-rust=y"
            echo "CONFIG_PACKAGE_shadowsocks-libev=y"
            echo "CONFIG_PACKAGE_trojan-go=y"
            echo "CONFIG_PACKAGE_naiveproxy=y"
            echo "CONFIG_PACKAGE_curl=y"
            echo "CONFIG_PACKAGE_unzip=y"
            echo "CONFIG_PACKAGE_ip-full=y"
            echo "CONFIG_PACKAGE_lua=y"
            echo "CONFIG_PACKAGE_luci-lib-jsonc=y"
            echo "CONFIG_PACKAGE_luci-compat=y"
            echo "CONFIG_PACKAGE_resolveip=y"
          } >> .config
          make defconfig

      - name: Build
        run: |
          make -j$(nproc) V=s

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: bin/targets/mediatek/filogic/*.bin
